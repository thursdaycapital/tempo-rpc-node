# Tempo RPC Node

A production-ready **Tempo RPC node deployment** with:

- Public RPC endpoint (via Cloudflare Tunnel)  
- `/health` JSON endpoint  
- `/status` professional dashboard  
- PM2 process manager for uptime  
- Works inside restricted networks (China / CGNAT / no public IP)

... (truncated for brevity in this example)
